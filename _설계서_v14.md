
---

## 최종 문안 감사 — 전체 텍스트를 뽑아 정독

14장의 사용자 노출 문자열을 전부 추출해 쪽 순서대로 읽었다. 실제 결함 5건.

| 쪽 | 결함 | 조치 |
|---|---|---|
| P.2 | `개설 속도를 결정하는 것은 … 접점 총량**이다**` — 덱 전체가 명사형·합쇼체인데 이 한 줄만 평서체 | `개설 속도를 결정하는 계약 이전 구간의 접점 총량` (명사형) |
| P.3 | `자영업자 1년 생존**률**` — 한글 어문규범상 ㄴ 받침 뒤는 '율' | `생존율` (원천 표기의 오기를 교정. 수치는 그대로) |
| P.5 | `예비창업자 인·적성검사 · 정보공개서부터 …` — 가운뎃점이 한 줄에서 **낱말 안(인·적성)과 항목 구분자로 동시에** 쓰여 4항 나열로 오독 | `인·적성검사, 정보공개서 제공부터 계약 체결까지 절차 관리` (쉼표로 층위 분리 + 앞 줄과 중복된 '예비창업자' 제거) |
| P.8 | `상권 · 손익 · 인·적성검사 결과로 …` — 같은 문제 | `상권 · 손익 자료와 인·적성검사 결과로 결정 마무리` |
| P.9 | 컬럼 헤더 `내일사장 · 착수 시점 0원` 과 결론 띠 태그 `착수 시점 0원` 이 **같은 장에 두 번** | 띠 태그 삭제 (띠 문장이 이미 완결) |

사실은 전부 원천과 일치했다 — 홈택스 연동(`:111`), KFA 1,400여개(`:125`),
SPC 가맹점 6,000개(`:126`), 바로고 18만(`:128`), 아프니까사장이다 210만(`:134`),
수행 브랜드 12종 + 외 18개 = 원천 30종(`:138`).

## 결론 띠와 본문 사이 여유 정합

P.2 좌측 각주 상자가 결론 띠 상단(6.13in)과 **0.01in 겹쳐 있었다.** 잉크는 겹치지
않아 렌더에서는 안 보였지만 잠재 결함이다. 항목 피치 0.48 → 0.46in, 각주 5.92 →
5.82in, 우측 카드 5.72 → 5.66in 으로 정리했다.

| | 이전 | 지금 |
|---|---|---|
| P.2 띠 위 잉크 여유 | 0.045in | **0.142in** |
| P.9 띠 위 잉크 여유 | 0.129in | 0.129in |

## 최종 검사 (배포본)

```
총 배치 615건 · 슬라이드 14장
영역 이탈 0 · 텍스트 넘침 0 · 겹침 0 · 텍스트 대비 위반 0
이미지 잉크 정합 33건 · 어긋남 0
이미지 잉크 대비 28건 · 위반 0
다크 면적 평균 1.6% · 결론 띠 2장 · 색 면 0장 / 14장 · 시각 면적 40% 미만 0장
```

**폐기한 검사** — 텍스트 상자 간 세로 간격 검사(`gapReport`)를 만들었으나 오탐 66건이
나왔다. 목록 항목·약력 줄처럼 **상자를 줄 높이에 맞춰 맞붙여 쌓은 조판**을 결함으로
잡았기 때문이다. 실제로 재보니 P.2 항목 간 잉크 여유는 0.278~0.290in 로 정상이었다.
상자 간격이 아니라 잉크 간격을 봐야 하므로, 렌더 실측 방식만 남기고 검사기는 되돌렸다.
