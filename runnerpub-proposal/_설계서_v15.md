# 가맹영업대행 제안서 v15 — 본사가동 라운드 A 적용

에이전시 직능으로 편성해 돌린 첫 라운드다. 62/62 에이전트 무오류 완주,
지적 53건 → 교차 반증 통과 35건(critical 3 · high 9 · medium 23).

빌드 : 총 배치 619건 · 14장 · 영역 이탈 0 · 넘침 0 · 겹침 0 · **가독 위반 0 (기준 AA 4.5)**

## 반증이 통과시켰지만 내가 직접 뒤집은 오진 2건

라운드가 올린 critical 1건 + high 1건을 PPTX XML 을 직접 열어 확인한 결과 **사실이 아니었다.**

| 지적 | 검증 |
|---|---|
| 결론 띠 대괄호 구간이 `C.YEL(#FFE96B)` 로 PPTX 안에 살아 있다 — 확정 조건 「노란 형광 0건」 위반 | **거짓.** `unzip → slide2/slide9.xml` 에서 `FFE96B` **0건**, 덱 전체 **0건**. `tx()` 가 `T.bandtx` 의 color 로 런 색을 덮어써 노란색이 XML 에 도달하지 않는다 |
| `runs()` 가 raw `\n` 을 런에 넣어 챕터 헤드 11장의 강조색이 소실·번짐 | **거짓.** pptxgenjs 가 `\n` 을 두 개의 `<a:p>` 로 분리하고, 1행 `#1E3457` / 2행 `#2868F0` 로 정상 분리돼 있다 |

교차 반증이 코드를 읽고 추론했을 뿐 산출물 XML 을 열지 않았다. **다음 라운드 반증 절차에
"PPTX XML 직접 확인" 을 추가해야 한다.**

## 사실 오류 — 원천 대조로 확정

**P.4 연혁이 1년 앞당겨져 있었다.** 원천 `_brief_naeilsajang.md:26` 의 2022.06~12 MVP
구간에는 다운로드 실적이 없고, `:34` 가 `연매출 2억원, 앱 다운로드 3만 돌파` 를 2023 구간에
둔다. 그런데 덱은 `앱 다운로드 3만 돌파` 를 2022 블록에 올려, 같은 덱 P.3 막대 라벨
(2023 = 3만)과 정면 충돌했다. → 2023 구간으로 이동, 2022 는 `앱 MVP 테스트 · 정식버전 출시 준비` 로.

**P.2 가 5,114명을 10.2배 부풀려 읽히게 했다.** `등급 분류된 예비창업자 DB 5,114명` 이라
적어 전원이 등급 분류를 마친 것으로 읽혔지만 원천의 상담 기록은 499건(9.8%)이다.
→ `예비창업자 DB 5,114명 · 상담 기록 499건` 으로 병기.

**P.6 헤드가 고객을 잘못 불렀다.** `소상공인 고객` 은 두 원천에 grep 0건이고, 소상공인은
기개업 사업자라 가맹 개설 대상(예비창업자)과 다르다. 같은 장 소제목은 `창업 수요` 라
불러 한 장에서 두 고객을 가리켰다. → `예비창업자 접점을 넓히는`.

**P.13 이 업종 경험을 암시했다.** `홀덤 업종 **전담** 영업 이력 없음` 은 전담이 아닌 홀덤
이력은 있다는 함의를 만든다 — 원천이 명시로 금지한 항목이고 같은 장 태그칩
(`업종 경험 없음`)과도 어긋났다. → `전담` 제거.

**P.5 실적 표기.** 원천은 개별 2주 실측값 2건(₩21,500,000 / ₩15,000,000)인데
`1,500~2,150만원` 연속 구간으로 적어 상시 주매출 밴드로 오독됐다.
→ `주간 실매출 2,150 / 1,500만원 2주 실측`.

**P.9 각주.** `일반 대행 — 착수금은 계약 실패 시에도 반환 없음` 은 제3자 계약 조건을
확인 불가한 사실로 단정한다. → `착수금 유무 및 반환 조건은 대행사별 계약에 따름`.

## 대비 — 검사 임계값을 AA 로 올리고 팔레트를 고쳤다

기존 `MIN_CONTRAST = 3.0` 은 **큰 글씨 기준**이라 8.5~11pt 각주·표를 놓치고 있었다.
AA 본문 기준 4.5 로 올리자 실제 위반이 드러났다.

| 색 | 배경 | 이전 | 지금 |
|---|---|---|---|
| `C.MUTE` | 흰색 | 4.44 ✗ | **5.61** (`6C7891` → `5C687F`) |
| `C.MUTE` | BLUEBG0 / GRAY / BLUEBG | 4.13 / 3.92 / 3.83 ✗ | **5.23 / 4.96 / 4.84** |
| P.9 강조 열 값 6칸 | BLUEBG | 4.19 ✗ | **5.64** (`C.BLUEDK 1A55CC` 신설) |
| 연혁 날짜칩 | BLUEBG | 4.19 ✗ | **5.64** (tagChip 이 자동 전환) |

## 전략·조판·프레이밍

- **P.14 전속 항목이 한 방향만 물었다.** `전속 여부 및 타 대행사 병행 여부` 는 본사→내일사장
  방향만 묻고, 내일사장이 홀덤 동종 브랜드를 병행하지 않는다는 축이 덱에 0건이었다
  (grep 동종·경업·배타 전부 0). 그런데 P.5 브랜드 벽은 투썸플레이스와 셀렉토커피를 같은
  격자에 나란히 놓아 그 우려를 스스로 키웠다. → `전속 여부 · 타 대행사 병행 · 홀덤 동종
  브랜드 영업 제한` 으로 양방향화. 새 약속을 지어내지 않고 본사 결정 항목으로 올렸다.
- **P.8 성공보수 판정 권한.** 대상 여부를 가르는 유일한 기준(`리드 최초 유입 경로`) 행의
  본사 승인 칸이 `—` 였다 — 돈 받는 쪽이 판정하고 본사는 승인권이 없다고 표가 적고 있었다.
  → `유입 경로 본사 확인`.
- **P.4 우측 소제목 띠가 존재하지 않았다.** 흰 지면에 `{bg:C.WHITE}` 를 깔아 면과 배경이
  모두 `#FFFFFF` 였다. → 기본 회색 틴트.
- **P.7 캡션 바 8모서리 삐짐.** 카드는 반경 0.08 라운드인데 캡션 바는 직각 `rect` 여서
  4장 × 2모서리에서 파란 면이 카드 실루엣 밖으로 나왔다. → 같은 반경 `rrect` + 상변만 직각.
- **하단 기준선.** P.13 좌우 단 0.567in 어긋남 → 항목 피치 0.46in 으로 6.61. P.11 하단
  흰 띠 1.07in → 6.43 → **6.69**. P.2 거터 좌 0.46 / 우 0.41 비대칭 → **0.46 / 0.46 대칭**.
- **P.9 · P.10 · P.13 중복 문구.** 3열 머리가 1행 값을 선점 / 소제목이 헤드를 되풀이 /
  표 앞 3행이 열 머리를 되풀이 — 전부 정리.
- **P.4 조직 고유명 띄어쓰기** `가맹사업 본부` → `가맹사업본부`, `영업 팀장` → `영업팀장` (원천 표기).
