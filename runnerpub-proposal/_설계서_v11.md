# 러너펍 제안서 v11 — 본사가동 R2 지시서 적용 기록

v10(커밋 `448ba72`)에 대해 검증 라운드 R2 를 돌렸다. 5개 렌즈(사실·설득·조판·시각·문장)가
지적 76건을 올렸고, 지적마다 독립 반증을 붙여 31건이 살아남았다. 회의 단계가 이를
중복·모순 제거해 **10개 action** 으로 수렴했고(critical 1 · high 4 · medium 5), 전량 적용했다.

빌드 : `node mk10.js` → 총 배치 **712건** · 영역 이탈 0 · 텍스트 넘침 0 · 겹침 0 · 가독 위반 0

---

## critical 1건

**A1 · P.8 STEP2 자산 재크롭** — R1 이 STEP1 에만 적용한 재크롭이 STEP2 에 누락돼 있었다.
`p07_step2.png` 상단에 앱 카드의 보라 라운드 테두리 잔재(y 0~22)가 남고 하단 체크박스 줄이
글자 한가운데서 잘렸다. `(0,24,1363,911)` 로 재크롭 → 1363×887. 좌표 검사는 이미지 *내부*
절단을 원리상 못 잡기 때문에 08쪽은 전 항목 통과 상태였다.

## high 4건

**A2 · P.13 준법 표** — 두 가지를 한 세트로 고쳤다.
① `업종 인허가 · 게임물 기준 — 러너펍 본사 기준 그대로 적용 · 내일사장 독자 판단 및 안내 금지` 행 신설.
박경관 대표의 1순위 반론("홀덤은 규제가 다르잖아")에 대한 답이 15장 어디에도 없었다.
새 사실을 만들지 않고 `_brief.md:43`(본사 승인 자료만 사용)에 근거한 약속만 진술한다.
② 얼룩 행이 괘선보다 0.32in 왼쪽으로 튀어나오던 것을 표 원점 이동 + 빈 첫 컬럼으로 정렬 복구.
flow 밴드 1.32→1.14in 로 줄여 6행 공간을 만들었다(카드 하단 3.780 불변, 여유 0.160in).

**A3 · P.9 · P.6 점주 자격 심사 노출** — `적성`·`심사`·`자격` 문자열이 덱 전체에 0건이었다.
"아무나 붙여서 부실 점주 깔 거 아니냐"가 가맹본부가 영업 위임을 거절하는 최빈 사유인데,
정작 **예비창업자 인·적성검사**(맥도날드·서브웨이 로직 분석·재해석, 국내 연구기관 공동개발)가
`_brief_naeilsajang.md:147` 에 보유 자산으로 실재한다. P.9 상담 행과 P.6 업무 불릿에 넣었다.
표 행은 늘리지 않았다 — 8행으로 늘리면 넘침으로 빌드가 중단된다.

**A4 · P.1 표지에 본사 이득 1행** — 표지 한 장에 비용이 두 번(42pt `1,000만원`, 다크 띠
`계약 성사 건에만 1,000만원`), 이득이 0번 나왔다. `500`·`흑자`·`순수취` 의 최초 등장이
12번째 장이라 `_brief.md:13` 설득축 3("본사는 계약 시점부터 흑자")이 표지에서 죽어 있었다.
`가맹비 1,500만원 − 성공보수 1,000만원 = 계약 시점 본사 순수취 +500만원` 을 11pt 본문으로
넣고(초대형 수치 1장 집중 규칙 유지), 각주에 `러너펍 공개 가맹 안내 기준` 을 병기했다.

**A5 · P.4 날조 각주 제거** — `* (23년 기준) 1년 생존률` 은 두 사실 원천 어디에도 없다.
동시에 `_brief_naeilsajang.md:86` 이 **두 값 모두에 반드시 병기하라**고 지정한 각주를
이 행에서만 밀어냈다. `* 내일사장 인증 서비스 이용 건 기준` 으로 교체.

## medium 5건

- **A6 · P.14** 파일럿 표도 P.13 과 같은 0.32in 어긋남이 있어 함께 정렬 복구
- **A7 · P.6** `주간 실매출 2,150만원 실측` → `1,500~2,150만원 기록`. 원천은 두 값을 병기하는데
  높은 쪽만 남겼고(차이 43%, 발신자에게 유리), 서술어도 `기록`→`실측` 으로 격상돼 있었다.
  같은 장 좌측 불릿이 `실측`을 현장 계측 의미로 쓰고 있어 오독을 만든다
- **A8 · P.6** 각주에 `2025~2026 영업 수행 브랜드 기준` 한정 복원 (원천 목록 제목의 한정)
- **A9 · P.5** 직함 2건 원상 복구 — `운영 팀장`→`팀장`, `김재현 기획팀장`→`김재현 팀장`.
  두 문자열 모두 원천에 없다. `_brief_naeilsajang.md:155` 직함 변경 금지 위반
- **A10 · P.1** 표지 폰 목업 재합성

## A10 상세 — v10 기록의 정정

`_설계서_v10.md` 는 표지 폰 목업이 "가로 절단 0"을 달성했다고 적었으나 **사실이 아니었다.**
`draw_assets7.py` 를 폭 기준(contain)으로 바꾼 것은 맞지만, 진짜 원인은 상위 소스
`assets/app_report.png` **자체가 우측이 잘린 캡처**라는 점이었다 — 헤드라인
`관심있는 매장, 안전할까?` 의 파란 픽셀이 파일 마지막 열(1651)까지 이어져 물음표가 파일 밖이다.
재크롭·재합성으로는 복원할 수 없다. 재캡처가 불가하므로 다음과 같이 처리했다.

1. `app_report_screen.png` 신설 — `(288, 690, 1652, 2656)` 크롭.
   좌측 288px 은 화면이 아니라 페이지 배경 네이비 띠였고(폰 안에 검은 세로줄로 들어가고 있었다),
   y 690 은 잘린 헤드라인 밴드(545~674) 바로 아래다.
2. `app_sales_screen.png` 신설 — `(64, 0, 1527, 2656)` 크롭. 같은 이유의 좌측 띠 제거.
3. `phone()` 의 세로 여백 처리를 흰색 단색 채움에서 **화면 자체의 위·아래 바탕색 잇기**로 교체.
   소스가 화면 슬롯보다 짧을 때 생기던 하단 흰 덩어리가 사라진다.

결과 : 두 대 모두 글줄 절단 0 · 흰 패딩 0 · 네이비 띠 0.

---

## 채택하지 않은 지적

| 지적 | 사유 |
|---|---|
| P.8 STEP2 배경 라벤더를 흰색으로 치환 | 앱 화면 자체의 배경이지 캔버스 색이 아니다. 색키 치환 시 폰 드롭섀도에 헤일로가 남는다 |
| P.6 각주에 사내 문서명(주간보고) 노출 | 외부 제안서에 사내 문서명을 쓰지 않는다. 기간 한정만 복원 |
| P.9 3열 `점주 최종 승인` | 아래 `계약 주선 — 가맹계약 체결` 행과 의미 충돌. `점주 승인` 으로 축약 |
| P.15 헤드를 `본사가 정하시는 항목 5건` 으로 | P.14 히어로가 이미 `본사가 정하시는 항목 8건` 이라 연속 두 장에 같은 문자열이 다른 숫자를 달게 된다 |
| P.14 `검증 기간`→`파일럿 기간` 통일 | 반증 통과했으나 `파일럿 운영 기준` 섹션 바 아래 첫 행이라 중복이 된다 |
| 폰 목업 "cover→contain 전환" 처방 | 이미 contain 이다. 원인 진단이 틀려 그대로 실행하면 결과가 안 바뀐다. A10 으로 대체 |

**사장님 확정 사항 저촉 0건** — 성공보수·청구 시점·네고 차감·LSM 3구간·인테리어 시공·
담당 공란·IR 어법·16:9 좌표 배치 어느 것도 건드리지 않았다. 발신/수신 방향 불변.
