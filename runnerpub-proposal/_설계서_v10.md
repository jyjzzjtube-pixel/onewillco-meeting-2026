# 러너펍 제안서 v10 — 본사가동 R1 지시서 적용 기록

v9(커밋 `af041d9`)에 대해 본사가동 멀티에이전트 검증 라운드 R1이 내린 지시서를
전량 적용한 판이다. R1 판정은 **"수정 필요 — 전달 불가"**, 지적 30건 중 채택 25건 ·
기각 12건이었다. 아래는 채택분의 적용 결과다.

빌드 : `node mk10.js` → `runnerpub_v10.pptx`
검사 : 총 배치 691건 · 영역 이탈 0 · 텍스트 넘침 0 · 겹침 0 · 가독 위반 0

---

## critical 7건

| # | 쪽 | 지적 | 적용 |
|---|---|---|---|
| 1 | P.4 | `↑ 22%` 는 산수 오류 (67→99 는 32%p) | `↑ 32%p` · `↑ 33%p` 로 교정, 단위도 %→%p |
| 2 | P.3·4·9 | `예비창업자 상담 DB 5,114명` 이 "상담 5,114건" 으로 읽힘 | 라벨을 `예비창업자 DB` 로, 각주를 `2026.06 사내 ERP 기준 · 상담 기록 499건` 으로 |
| 3 | P.4·5 | 빈 빨간 삼각형이 슬래브로 렌더 · 덱에 적색 잔존 | `tri()` 2회 삭제, 델타·연혁 3줄 `C.RED`→`C.BLUE` (덱 전체 적색 0건) |
| 4 | P.12 | 틴트면이 상단 괘선·로고·쪽번호를 덮음 | `page()` 대신 `addSlide → bg → grad → frame → anchor` 순서로 재작성 |
| 5 | P.5 | 헤드가 우측 팀 표를 눌러 표가 2.58in 부터 시작 | 헤드를 좌측 틴트면으로 이동, 팀 표 시작 2.58→2.12in, 틴트 경계 5.30→5.86in |
| 6 | P.8 | STEP1 이미지가 글줄 한가운데를 자름 | `app_sales.png` 를 (64,262)–(1525,1128) 로 재크롭 |
| 7 | P.6 | `대표 사례` 가 실적 규모를 과장 | `진행 사례` 로, 카드에 `주간 실매출 2,150만원 실측` 추가, 각주를 원장 기준 고지로 |

## high 10건

- P.8 · P.15 쪽번호 복구 — P.8 은 틴트면을 `frame()` 앞으로, P.15 는 수동 배치
- P.9 `본사 유입 건` 행 회색 해제 + `성공보수 대상 제외 · 리드 최초 유입 경로 기준`
- P.5 헤드를 `[가맹 개설 · 영업]을 직접 수행한` 으로 (본사 수행 주체 혼동 제거)
- P.13 `법정 숙려기간`→`법정 숙고기간`, 근거를 `가맹사업법 제7조제3항` 으로 특정
- P.11 등식 박스(P.12 와 중복) 삭제 → `할인 귀속` · `해제 시 환수` 2행으로 교체
- P.14 `보완 장치 3종` → `파일럿 · 주 단위 리포트 · 본사 승인 자료 전용` 실제 열거
- P.14 표 6행→8행 (`영업 범위` · `중단 기준` 신설), 히어로 수치 `6 건`→`8건`
- P.9 표 6행→7행 (`인테리어 시공` 신설 — 브리프 §4 반영)
- P.6~P.10 섹션 라벨에 러너펍 물림 (누구를 위한 표인지 매 장 명시)
- P.7 제휴 로고 7종 배치 + 3:5 배경 위계 (SPC·KFA·바로고 강조 틴트 + 청색 액센트)

## medium 8건

- P.10 결론 띠 명사형 재작성 + 우측 `착수 시점 0원` 태그
- P.1 다크 띠에 제안 4행 요약 (확보된 수요 · 위임 범위 · 비용 구조 · 검증 방식)
- P.1 표지 폰 목업 재제작 — 화면 맞춤을 cover→폭 기준으로 바꿔 **가로 절단 0**
- P.3 하단 카드 문안 조정
- P.14 히어로 패널 `C.GRAY`→`C.BLUEBG0`
- P.15 사인오프 위 4.20in 중앙 괘선
- 단위 붙임 통일 (`1,000 만원`→`1,000만원`, `67 %`→`67%`, `0 건`→`0건`)
- 틴트면 하드심에 1px 괘선 (P.3 · P.5 · P.12)

## 기각 12건 (요지)

챕터 재분할 · 페이지 순서 재배열 · MIN_CONTRAST 4.5 상향 · 자간 전면 재조판 ·
가맹금 예치제를 반영한 청구 시점 변경 등. 사장님 확정 조건(브리프 §4)을 바꾸거나,
IR 어법 이식 결과를 되돌리거나, 근거 없는 사실을 새로 만드는 요구여서 적용하지 않았다.

---

## 사실 원천

모든 수치·문구는 `_brief.md` · `_brief_naeilsajang.md` 안에서만 취했다.
`주간 실매출 2,150만원` 은 `_brief_naeilsajang.md:140` (33떡볶이 강동역점 ₩21,500,000),
`숙고기간` 표기는 같은 문서 97·115행, `인테리어 시공` 은 `_brief.md:42` 근거다.
담당·연락처·이메일은 지시대로 공란을 유지했고, 홀덤 업종 수행 이력은 P.14 에서
`0건` 으로 먼저 밝혔다.
